

export class Block {
    public name: string;
    public percent: number;
    public sum: number;

    constructor(name: string, percent: number, sum: number) {
        this.name = name;
        this.percent = percent;
        this.sum = sum;
    }
}
